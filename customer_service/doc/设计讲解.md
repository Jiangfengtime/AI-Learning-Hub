
## 1. 实现DM的DST -> Tracker类
### 1. 属性
1. 用户会话级别隔离 -> sender_id
2. 保存历史 -> Turns -> List<Turn> -> Turn(User + List<Bot>)
3. 保存Slot -> slots -> List<Slot> 
4. 保存最新用户消息 -> UserMessage类型
5. 保存对话栈 -> Stack
6. 其他：
### 2. 函数
#### 对话历史
1. updateMessage： 
   1. 保存上一个Turn，
   2. 当前Turn的用户消息(此时还没生成bot消息)
2. addBotMessage
   1. 内部处理完，生成bot消息，准备发出去
3. saveCurrentTurn
   1. 将当前turn保存到Turns中
4. getConversationHistory
   1. 获取历史对话(Turns + currentTurn)

#### Slot
   1. 读取：指定slot、所有slot
   2. 写入：更新
   3. 重置所有slot
#### 对话栈


#### 持久化

